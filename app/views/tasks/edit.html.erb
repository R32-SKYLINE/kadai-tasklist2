<h1>id: <%= @work.id %> のタスク編集ページ</h1>

<%= form_with(model: @work) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '設定' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>